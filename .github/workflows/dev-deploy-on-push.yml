name: Dev Deploy On Push

on:
  push:
    branches:
      - develop
    paths:
      - ".github/workflows/dev-deploy-on-push.yml"
      - ".github/actions/dev-deploy-run/**"
      - ".github/actions/materialize-maven-settings/**"
      - ".github/deploy/backend/**"
      - "boot/**"
      - "api-rest/**"
      - "application/**"
      - "domain/**"
      - "clients/**"
      - "Dockerfile"
      - "pom.xml"
      - "README.md"
      - "docs/**"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: dev
    env:
      DEPLOY_VM_PORT: ${{ vars.DEPLOY_VM_PORT }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}

      - name: Execute dev deployment
        uses: ./.github/actions/dev-deploy-run
        with:
          github_environment: dev
        env:
          DEPLOY_VM_PORT: ${{ vars.DEPLOY_VM_PORT }}
          DEPLOY_VM_HOST: ${{ secrets.DEPLOY_VM_HOST }}
          DEPLOY_VM_USER: ${{ secrets.DEPLOY_VM_USER }}
          DEPLOY_VM_SSH_PRIVATE_KEY: ${{ secrets.DEPLOY_VM_SSH_PRIVATE_KEY }}
          FORGE_SECURITY_DEV_JWT_SECRET: ${{ secrets.FORGE_SECURITY_DEV_JWT_SECRET }}
          GHCR_PULL_USERNAME: ${{ secrets.GHCR_PULL_USERNAME }}
          GHCR_PULL_TOKEN: ${{ secrets.GHCR_PULL_TOKEN }}
          MAVEN_REPOSITORY_USERNAME: ${{ vars.MAVEN_REPOSITORY_USERNAME }}
          MAVEN_REPOSITORY_TOKEN: ${{ secrets.MAVEN_REPOSITORY_TOKEN }}
